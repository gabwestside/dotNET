using System;
using System.Globalization;

namespace BeeCrowdChallengesHomeEdition
{
    class Program
    {
        static void Main(string[] args)
        {
            // double radius, raidusPowered, pi = 3.14159;

            // radius = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);

            // raidusPowered = ((4.0/3) * pi * Math.Pow(radius, 3));

            // Console.WriteLine($"VOLUME = {raidusPowered.ToString("F3", CultureInfo.InvariantCulture)}");

            
        }
    }
}
